# AWESOMERUBY TEST FOR 42.WORKS

## Tech
* Ruby 3.0.3
* Rails 7
* pg 1.3.5

## Setup
* ```git clone```
* ```bundle install```
* ```cp .env.example .env```
*    Set your own token into .env
* ```rails s```
* ```sidekiq -C config/sidekiq.yml```
* ```rake fetch```
